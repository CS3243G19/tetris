repositories {
    mavenCentral()
}

apply plugin: "java"
apply plugin: "application"
apply plugin: "checkstyle"

mainClassName = "tetris.PlayerSkeleton"

task score(type: GradleBuild) {
     mainClassName = "tetris.scorer.Scorer"
     tasks = ['run']
}